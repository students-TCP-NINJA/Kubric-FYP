# Sample Service

Minimal Flask microservice used as a scaffold for CI, container image, and Kubernetes deployment testing.

Run locally:

```bash
python app.py
```

Build image:

```bash
docker build -t sample-service:latest .
```
